version: '3.8'

services:
  portfolio:
    container_name: sizuwanoadi/sizu-portfolio
    image: sizuwanoadi/sizu-portfolio:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
